# Story 3 — Personalization

**Status:** Done
**Epic:** 3
**Kiro Reqs:** R3.2, R4.3, R7.4

## AC
1. Settings page at `/settings` with level, accent, TTS voice picker.
2. Voice picker lists all English voices from `speechSynthesis.getVoices()`.
3. "Test" button speaks a sample line with the selected voice.
4. Settings persist via Zustand `persist` middleware (localStorage).
5. `/api/chat` injects level + accent into the system prompt.

## Files
- `app/settings/page.tsx`
- `lib/store.ts` (`useSettings`)
- `lib/prompts/tutor.ts`

## Verdict
QA: PASS — typecheck + build clean.
