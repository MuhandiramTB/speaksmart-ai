# Story 4 — Progress Dashboard (local-first)

**Status:** Done
**Epic:** 4
**Kiro Reqs:** R8.3 (local variant), R9.1–9.3

## Decision
v1 stores sessions in localStorage (Zustand `persist`) — true to the "no sign-up needed" promise. Supabase schema + RLS are committed at `supabase/schema.sql` for cross-device sync in v2.

## AC
1. `/dashboard` shows totals, 7-day avg pronunciation, grammar/100 words, streak.
2. Bar chart of last 14 sessions' pronunciation scores.
3. Recent sessions table (last 20).
4. Empty state when < 3 sessions completed.
5. "Clear history" button with confirm prompt.

## Files
- `app/dashboard/page.tsx`
- `lib/store.ts` (`useHistory`, `PastSession`)
- `components/practice/PracticeRoom.tsx` (`finishAndExit` writes to history)

## Verdict
QA: PASS — typecheck + build clean.
