# Story 2 — Feedback Layer (Pronunciation + Grammar)

**Status:** Done
**Epic:** 2
**Kiro Reqs:** R5.1–5.4, R6.1–6.4

## AC
1. Pronunciation score (0–100) shown next to every user message.
2. Weak words underlined; clicking speaks them via TTS at 0.85x rate.
3. Grammar API returns JSON `{ isCorrect, corrected, explanation }`.
4. Diff card rendered beneath user message when grammar is incorrect.
5. "Good grammar" pill when correct.

## Files
- `app/api/transcribe/route.ts` — scoring + weak-word detection from segment logprobs
- `app/api/grammar/route.ts` — JSON-mode Llama 3.3 prompt
- `components/practice/ChatThread.tsx` — `MessageBubble` + `GrammarBadge`
- `components/practice/PracticeRoom.tsx` — parallel `chatPromise` + `grammarPromise`

## Verdict
QA: PASS — typecheck + build clean. Manual smoke pending Groq key.
