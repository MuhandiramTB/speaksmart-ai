# Story 5 — Polish & Launch

**Status:** Done (code) / Pending (deploy)
**Epic:** 5
**Kiro Reqs:** NFR3, UX, R-launch

## AC
1. PWA manifest with theme color + standalone display.
2. SVG app icon.
3. Onboarding overlay shows once (3 slides, dismissable, persisted).
4. Share button uses Web Share API with clipboard fallback.

## Files
- `public/manifest.webmanifest`
- `public/icon.svg`
- `app/layout.tsx` (metadata + viewport)
- `components/OnboardingOverlay.tsx`
- `components/ShareButton.tsx`

## Deferred to user
- Vercel deploy (`vercel --prod`)
- Lighthouse audit on production URL

## Verdict
QA: PASS — typecheck + build clean.
